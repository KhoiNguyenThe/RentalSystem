<Window x:Class="RentalSystem.Homepage.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RentalSystem.Homepage"
        mc:Ignorable="d"
       Title="Register - Rental System"
        Height="520" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f9f9f9">

    <Grid Margin="20">
        <Border CornerRadius="12" Background="White" Padding="25" VerticalAlignment="Center">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Create Account"
                               FontSize="26" FontWeight="Bold"
                               Foreground="#0078D7"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,15"/>

                    <TextBlock Text="Full Name:" FontSize="14"/>
                    <TextBox x:Name="txtName" Height="20" Margin="0,5,0,10"/>

                    <TextBlock Text="Email:" FontSize="14"/>
                    <TextBox x:Name="txtEmail" Height="20" Margin="0,5,0,10"/>

                    <TextBlock Text="Password:" FontSize="14"/>
                    <PasswordBox x:Name="txtPassword" Height="20" Margin="0,5,0,15"/>

                
                    <TextBlock Text="Citizen ID Photo:" FontSize="14" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,15">
                        <Button x:Name="btnUpload" Content="📁 Upload Image" Width="130" Margin="5,0"
                                Click="btnUpload_Click"/>
                        <Button x:Name="btnCapture" Content="📷 Capture" Width="100" Margin="5,0"
                                Click="btnCapture_Click"/>
                    </StackPanel>

                    <Border BorderBrush="Gray"
        BorderThickness="1"
        HorizontalAlignment="Center"
        Margin="0,5,0,15"
        Width="160" Height="120">
                        <Image x:Name="imgPreview"
           Stretch="UniformToFill"/>
                    </Border>
                    <Button Content="Register"
                            Width="150" Height="35"
                            Background="#0078D7" Foreground="White"
                            FontWeight="Bold"
                            HorizontalAlignment="Center"
                            Click="btnRegister_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>